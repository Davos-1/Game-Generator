---
import BaseLayout from '@/layouts/BaseLayout.astro';
import PuzzleGenerator from '@/components/PuzzleGenerator';
import { t } from '@/i18n';
---

<BaseLayout title={t('nav.sudoku')} description={t('home.cards.sudoku')}>
  <h1 class="text-3xl font-bold tracking-tight">{t('nav.sudoku')}</h1>
  <p class="mt-2 max-w-2xl text-slate-600">{t('home.cards.sudoku')}</p>
  <div class="mt-8">
    <PuzzleGenerator kind="sudoku" client:load />
  </div>
</BaseLayout>
